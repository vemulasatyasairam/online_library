# Online Library backend

Simple Express server for the Online Library project.

Run:

```powershell
cd "c:\sai ram html\Online Library project\pages\backend"
npm install
npm start
```

Server will listen on port 5050 (http://localhost:5050).
